# Omakase AI - Development Dockerfile
FROM node:20-alpine

WORKDIR /app

# Install dependencies
COPY package*.json ./
RUN npm install

# Copy source code
COPY . .

ENV NODE_ENV=development
ENV PORT=3000

EXPOSE 3000 5173

CMD ["npm", "run", "dev"]
